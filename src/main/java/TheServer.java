import java.net.*;
import java.io.*;

public class TheServer {
    // server should listen on a port
    // when server starts up it should say what port it's listening on
    // should take incoming requests and echo back the request
    // http URL needs host Name, and port number
    // URL.getPath
    // URL.getQuery
    // URL.openStream reads directly from a URL (returns an InputStream)
    // BufferedReader Reads directly from the Inputstream
}
